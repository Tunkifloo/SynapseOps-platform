# Cargado por Vitest (mode = test). Evita que src/shared/api/env.ts lance en CI,
# donde frontend/.env (gitignored) no existe. El valor no afecta a los tests
# (las llamadas de red se mockean); solo satisface la validación de env.ts.
VITE_API_BASE_URL=http://localhost:8080/api/v1
