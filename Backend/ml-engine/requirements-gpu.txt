# Lista COMPLETA GPU/CUDA para desarrollo local:  pip install -r requirements-gpu.txt
#
# El Dockerfile usa las capas separadas (ver requirements.txt). No edites pins aquí:
#   - frameworks (TF/Torch CUDA) → requirements-frameworks-gpu.txt
#   - deps de app compartidas     → requirements-core.txt
-r requirements-frameworks-gpu.txt
-r requirements-core.txt
