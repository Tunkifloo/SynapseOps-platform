# Lista COMPLETA CPU para desarrollo local:  pip install -r requirements.txt
#
# Se compone de dos archivos para que el Dockerfile pueda instalar por CAPAS separadas
# (frameworks pesados + deps de app ligeras) y cachear mejor. No edites pins aquí:
#   - frameworks (TF/Torch CPU) → requirements-frameworks.txt
#   - deps de app compartidas    → requirements-core.txt
-r requirements-frameworks.txt
-r requirements-core.txt
