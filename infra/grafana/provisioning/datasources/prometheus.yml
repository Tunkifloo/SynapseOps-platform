apiVersion: 1

datasources:
  - name: Prometheus
    type: prometheus
    uid: prometheus          # uid fijo → referenciado por el dashboard provisionado
    access: proxy
    url: http://prometheus-tsdb:9090
    isDefault: true
    editable: false